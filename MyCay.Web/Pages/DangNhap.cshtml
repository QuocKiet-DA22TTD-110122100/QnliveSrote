@page
@model DangNhapModel
@{
    ViewData["Title"] = "Đăng nhập";
}

<section class="auth-page">
    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-header">
                <div class="auth-logo">🌶️</div>
                <h1>Đăng nhập</h1>
                <p>Chào mừng bạn quay trở lại!</p>
            </div>

            <!-- Social Login -->
            <div class="social-login">
                <button type="button" class="btn-social btn-google" onclick="loginWithGoogle()">
                    <svg viewBox="0 0 24 24" width="20" height="20">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                    </svg>
                    Đăng nhập với Google
                </button>
                <button type="button" class="btn-social btn-facebook" onclick="loginWithFacebook()">
                    <svg viewBox="0 0 24 24" width="20" height="20" fill="#1877F2">
                        <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                    </svg>
                    Đăng nhập với Facebook
                </button>
            </div>

            <div class="divider">
                <span>hoặc đăng nhập với email</span>
            </div>

            <!-- Login Form -->
            <form id="loginForm" class="auth-form">
                <div class="form-group">
                    <label for="email">Email hoặc số điện thoại</label>
                    <input type="text" id="email" name="email" placeholder="email@example.com" required />
                </div>

                <div class="form-group">
                    <label for="password">Mật khẩu</label>
                    <div class="password-input">
                        <input type="password" id="password" name="password" placeholder="Nhập mật khẩu" required />
                        <button type="button" class="toggle-password" onclick="togglePassword()">👁️</button>
                    </div>
                </div>

                <div class="form-options">
                    <div class="remember-me">
                        <input type="checkbox" id="rememberMe" name="rememberMe" />
                        <label for="rememberMe">Ghi nhớ đăng nhập</label>
                    </div>
                    <a href="/QuenMatKhau" class="forgot-password">Quên mật khẩu?</a>
                </div>

                <button type="submit" class="btn-submit">
                    Đăng nhập
                </button>
            </form>

            <div class="auth-footer">
                <p>Chưa có tài khoản? <a href="/DangKy">Đăng ký ngay</a></p>
            </div>
        </div>

        <div class="auth-banner">
            <div class="banner-content">
                <h2>Mỳ Cay Sasin</h2>
                <p class="banner-tagline">Hương vị Hàn Quốc chính hiệu</p>
                <div class="banner-features">
                    <div class="feature">
                        <span class="feature-icon">🔥</span>
                        <span>7 cấp độ cay</span>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">🚀</span>
                        <span>Giao hàng 30 phút</span>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">⭐</span>
                        <span>10,000+ đánh giá 5 sao</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
<script>
    function togglePassword() {
        const input = document.getElementById('password');
        input.type = input.type === 'password' ? 'text' : 'password';
    }

    document.getElementById('loginForm').addEventListener('submit', async function(e) {
        e.preventDefault();
        
        const email = document.getElementById('email').value.trim();
        const password = document.getElementById('password').value;

        // Gọi API đăng nhập
        try {
            const response = await fetch('/api/auth/login', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email, password })
            });
            
            const result = await response.json();
            
            if (result.success) {
                loginSuccess(result.data.email, result.data.name, result.data.role, result.data.token, result.data.id);
            } else {
                // Fallback: kiểm tra localStorage
                checkLocalStorage(email, password);
            }
        } catch (error) {
            console.error('Login API error:', error);
            // Fallback: kiểm tra localStorage nếu API lỗi
            checkLocalStorage(email, password);
        }
    });

    function checkLocalStorage(email, password) {
        // Demo accounts
        const demoAccounts = {
            'admin@mycaysasin.vn': { password: '123456', role: 'admin', name: 'Nguyễn Văn Admin' },
            'admin': { password: '123456', role: 'admin', name: 'Nguyễn Văn Admin' },
            'quanly1@mycaysasin.vn': { password: '123456', role: 'manager', name: 'Trần Thị Quản Lý' },
            'quanly1': { password: '123456', role: 'manager', name: 'Trần Thị Quản Lý' },
            'nhanvien1@mycaysasin.vn': { password: '123456', role: 'staff', name: 'Lê Văn Nhân Viên' },
            'nhanvien1': { password: '123456', role: 'staff', name: 'Lê Văn Nhân Viên' },
            'khach1@gmail.com': { password: '123456', role: 'customer', name: 'Nguyễn Thị Mai' },
            'khachhang1': { password: '123456', role: 'customer', name: 'Nguyễn Thị Mai' }
        };

        if (demoAccounts[email] && demoAccounts[email].password === password) {
            loginSuccess(email, demoAccounts[email].name, demoAccounts[email].role);
            return;
        }

        // Kiểm tra tài khoản đã đăng ký trong localStorage
        const registeredUsers = JSON.parse(localStorage.getItem('registeredUsers') || '[]');
        const user = registeredUsers.find(u => 
            (u.email === email || u.phone === email) && u.password === password
        );

        if (user) {
            loginSuccess(user.email, user.fullName, user.role, user.phone);
        } else {
            showToast('❌ Email/SĐT hoặc mật khẩu không đúng!', 'error');
        }
    }

    function loginSuccess(email, name, role, token = '', id = null) {
        localStorage.setItem('user', JSON.stringify({
            id: id,
            username: email,
            email: email,
            name: name,
            role: role,
            token: token
        }));
        showToast('✓ Đăng nhập thành công!', 'success');
        
        // Lấy returnUrl nếu có
        const urlParams = new URLSearchParams(window.location.search);
        const returnUrl = urlParams.get('returnUrl');
        
        setTimeout(() => {
            if (returnUrl) {
                window.location.href = returnUrl;
            } else if (role === 'admin' || role === 'manager' || role === 'staff') {
                window.location.href = '/Admin';
            } else {
                window.location.href = '/';
            }
        }, 1500);
    }

    function loginWithGoogle() {
        showToast('Đang kết nối với Google...', 'info');
    }

    function loginWithFacebook() {
        showToast('Đang kết nối với Facebook...', 'info');
    }

    function showToast(message, type = 'info') {
        const toast = document.createElement('div');
        toast.className = 'toast-message toast-' + type;
        toast.textContent = message;
        document.body.appendChild(toast);
        setTimeout(() => toast.remove(), 3000);
    }
</script>
}
