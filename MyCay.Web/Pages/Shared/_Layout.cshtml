<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Mỳ Cay Sasin</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🌶️</text></svg>">
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <!-- Header - Optimized -->
    <header class="header" id="mainHeader">
        <nav class="nav-container">
            <!-- Logo với icon ớt -->
            <a href="/" class="logo">
                <span class="logo-icon">🌶️</span>
                <span class="logo-text">Mỳ Cay <span class="logo-accent">Sasin</span></span>
            </a>
            
            <!-- Menu chính - Giảm xuống 5 item -->
            <ul class="nav-menu" id="navMenu">
                <li><a href="/" class="nav-link">Trang chủ</a></li>
                <li><a href="/ThucDon" class="nav-link">Thực đơn</a></li>
                <li><a href="/UuDai" class="nav-link">Ưu đãi</a></li>
                <li class="nav-dropdown">
                    <a href="#" class="nav-link dropdown-toggle">Khác <i class="fas fa-chevron-down"></i></a>
                    <div class="dropdown-menu">
                        <a href="/TinTuc"><i class="fas fa-newspaper"></i> Tin tức</a>
                        <a href="/DanhGia"><i class="fas fa-star"></i> Đánh giá</a>
                        <a href="/LienHe"><i class="fas fa-phone"></i> Liên hệ</a>
                    </div>
                </li>
            </ul>
            
            <!-- Right side actions -->
            <div class="nav-actions">
                <!-- Search Icon -->
                <button class="nav-icon-btn search-toggle" id="searchToggle" onclick="toggleSearch()" title="Tìm kiếm (Ctrl+K)">
                    <i class="fas fa-search"></i>
                </button>
                
                <!-- Đăng nhập - Text link nhỏ -->
                <a href="/DangNhap" class="nav-login-link" id="loginLink">
                    <i class="fas fa-user"></i>
                    <span>Đăng nhập</span>
                </a>
                
                <!-- User Menu (khi đã đăng nhập) -->
                <div id="userMenu" class="user-menu-item" style="display: none;">
                    <div class="user-dropdown-nav">
                        <button class="user-nav-btn" title="Tài khoản">
                            <i class="fas fa-user-circle user-icon"></i>
                        </button>
                        <div class="user-dropdown-content">
                            <div class="user-dropdown-header">
                                <span class="user-greeting">Xin chào,</span>
                                <span class="user-name" id="navUserName">User</span>
                            </div>
                            <hr>
                            <a href="/TaiKhoan"><i class="fas fa-user"></i> Tài khoản</a>
                            <a href="/DonHangCuaToi"><i class="fas fa-shopping-bag"></i> Đơn hàng của tôi</a>
                            <a href="/Admin" id="adminLink" style="display: none;"><i class="fas fa-cog"></i> Quản trị</a>
                            <hr>
                            <a href="#" onclick="logoutUser()" class="logout-link"><i class="fas fa-sign-out-alt"></i> Đăng xuất</a>
                        </div>
                    </div>
                </div>
                
                <!-- Icon Giỏ hàng riêng -->
                <a href="/GioHang" class="nav-cart-btn" title="Giỏ hàng">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="cart-count" id="cartCount">0</span>
                </a>
                
                <!-- NÚT ĐẶT MÓN NGAY - CTA chính với badge -->
                <a href="/ThucDon" class="btn-order-cta" id="orderCTA">
                    <i class="fas fa-utensils"></i>
                    <span class="btn-order-text">Đặt món ngay</span>
                    <span class="btn-order-badge">-15%</span>
                </a>
            </div>
            
            <!-- Mobile Menu Button -->
            <button class="mobile-menu-btn" id="mobileMenuBtn" onclick="toggleMobileMenu()">
                <i class="fas fa-bars"></i>
            </button>
        </nav>
        
        <!-- Search Dropdown -->
        <div class="search-dropdown" id="searchDropdown">
            <div class="search-input-wrapper">
                <i class="fas fa-search"></i>
                <input type="text" id="globalSearch" placeholder="Tìm kiếm món ăn..." autocomplete="off" />
                <button class="search-close" onclick="toggleSearch()">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="search-results" id="searchResults"></div>
        </div>
        
        <!-- Free ship banner -->
        <div class="header-promo-bar">
            <span>🚚 Miễn phí ship đơn từ 200k</span>
            <span class="promo-divider">|</span>
            <span>⏰ Giao hàng 30 phút</span>
        </div>
    </header>
    
    <!-- Mobile Menu Overlay -->
    <div class="mobile-menu-overlay" id="mobileMenuOverlay" onclick="toggleMobileMenu()"></div>
    <div class="mobile-menu" id="mobileMenu">
        <div class="mobile-menu-header">
            <span class="logo-icon">🌶️</span>
            <span>Mỳ Cay Sasin</span>
            <button onclick="toggleMobileMenu()"><i class="fas fa-times"></i></button>
        </div>
        <nav class="mobile-nav">
            <a href="/"><i class="fas fa-home"></i> Trang chủ</a>
            <a href="/ThucDon"><i class="fas fa-utensils"></i> Thực đơn</a>
            <a href="/UuDai"><i class="fas fa-gift"></i> Ưu đãi</a>
            <a href="/TinTuc"><i class="fas fa-newspaper"></i> Tin tức</a>
            <a href="/DanhGia"><i class="fas fa-star"></i> Đánh giá</a>
            <a href="/GioHang"><i class="fas fa-shopping-cart"></i> Giỏ hàng <span class="mobile-cart-badge" id="cartCountMobile">0</span></a>
        </nav>
        <a href="/ThucDon" class="mobile-order-btn">
            <i class="fas fa-shopping-cart"></i> Đặt món ngay - Giảm 15%
        </a>
        <div class="mobile-login" id="mobileLoginArea">
            <a href="/DangNhap" class="mobile-login-btn">Đăng nhập / Đăng ký</a>
        </div>
    </div>

    <main>
        @RenderBody()
    </main>

    <!-- Footer - Compact Horizontal -->
    <footer style="background: linear-gradient(180deg, #1f2937 0%, #111827 100%); color: white; padding: 15px 0 0;">
        <div style="max-width: 1200px; margin: 0 auto; padding: 0 20px;">
            <div style="display: flex !important; flex-direction: row !important; flex-wrap: nowrap !important; justify-content: space-between !important; gap: 30px; padding-bottom: 12px; border-bottom: 1px solid rgba(255,255,255,0.1); font-size: 11px;">
                <!-- Brand -->
                <div style="flex: 1.5; min-width: 180px;">
                    <div style="display: flex; align-items: center; gap: 5px; margin-bottom: 6px;">
                        <span style="font-size: 14px;">🌶️</span>
                        <span style="font-size: 13px; font-weight: 700;">Mỳ Cay <span style="color: #f97316;">Sasin</span></span>
                    </div>
                    <p style="color: rgba(255,255,255,0.7); line-height: 1.3; margin: 0 0 8px 0; font-size: 10px;">Hương vị Hàn Quốc chính hiệu.</p>
                    <div style="display: flex; gap: 5px;">
                        <a href="#" style="width: 22px; height: 22px; background: #1877f2; border-radius: 5px; display: inline-flex; align-items: center; justify-content: center; color: white; font-size: 9px;"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" style="width: 22px; height: 22px; background: linear-gradient(45deg, #f09433, #dc2743); border-radius: 5px; display: inline-flex; align-items: center; justify-content: center; color: white; font-size: 9px;"><i class="fab fa-instagram"></i></a>
                        <a href="#" style="width: 22px; height: 22px; background: #000; border-radius: 5px; display: inline-flex; align-items: center; justify-content: center; color: white; font-size: 9px;"><i class="fab fa-tiktok"></i></a>
                    </div>
                </div>
                <!-- Links -->
                <div style="flex: 0.8; min-width: 80px;">
                    <h4 style="font-size: 11px; font-weight: 600; margin: 0 0 6px 0;">Liên kết</h4>
                    <div><a href="/" style="color: rgba(255,255,255,0.7); text-decoration: none; display: block; margin-bottom: 3px;">Trang chủ</a></div>
                    <div><a href="/ThucDon" style="color: rgba(255,255,255,0.7); text-decoration: none; display: block; margin-bottom: 3px;">Thực đơn</a></div>
                    <div><a href="/UuDai" style="color: rgba(255,255,255,0.7); text-decoration: none; display: block;">Ưu đãi</a></div>
                </div>
                <!-- Contact -->
                <div style="flex: 1.2; min-width: 150px;">
                    <h4 style="font-size: 11px; font-weight: 600; margin: 0 0 6px 0;">Liên hệ</h4>
                    <p style="color: rgba(255,255,255,0.7); margin: 0 0 3px 0;"><i class="fas fa-phone-alt" style="color: #f97316; margin-right: 5px; font-size: 9px;"></i><a href="tel:19001234" style="color: #f97316; text-decoration: none; font-weight: 600;">1900-1234</a></p>
                    <p style="color: rgba(255,255,255,0.7); margin: 0 0 3px 0;"><i class="fas fa-envelope" style="color: #f97316; margin-right: 5px; font-size: 9px;"></i>info@mycaysasin.vn</p>
                    <p style="color: rgba(255,255,255,0.7); margin: 0;"><i class="fas fa-map-marker-alt" style="color: #f97316; margin-right: 5px; font-size: 9px;"></i>Q.1, TP.HCM</p>
                </div>
                <!-- Hours -->
                <div style="flex: 0.8; min-width: 80px;">
                    <h4 style="font-size: 11px; font-weight: 600; margin: 0 0 6px 0;">Giờ mở cửa</h4>
                    <p style="color: rgba(255,255,255,0.7); margin: 0 0 2px 0;">T2 - CN</p>
                    <p style="color: #f97316; font-weight: 600; margin: 0;">10:00 - 22:00</p>
                </div>
            </div>
        </div>
        <div style="display: flex !important; justify-content: space-between !important; align-items: center !important; padding: 8px 20px; max-width: 1200px; margin: 0 auto; font-size: 10px;">
            <p style="color: rgba(255,255,255,0.5); margin: 0;">&copy; 2024 Mỳ Cay Sasin</p>
            <div style="display: flex; gap: 12px;">
                <a href="#" style="color: rgba(255,255,255,0.5); text-decoration: none;">Chính sách</a>
                <a href="#" style="color: rgba(255,255,255,0.5); text-decoration: none;">Điều khoản</a>
            </div>
        </div>
    </footer>
    
    <style>
    
    @@media (max-width: 1024px) {
        .footer-main { grid-template-columns: 1fr 1fr; gap: 25px; }
    }
    @@media (max-width: 640px) {
        .footer-new { padding-top: 30px; }
        .footer-main { grid-template-columns: 1fr; gap: 20px; }
        .footer-bottom-new { flex-direction: column; gap: 8px; text-align: center; padding: 12px 20px; }
    }
    </style>

    <style>
    /* ========== NEW HEADER STYLES ========== */
    .header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        background: rgba(0, 0, 0, 0.9);
        backdrop-filter: blur(10px);
        z-index: 1000;
        transition: all 0.3s ease;
    }
    .header.scrolled {
        background: rgba(0, 0, 0, 0.95);
        box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }
    .nav-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 12px 20px;
        display: flex;
        align-items: center;
        gap: 30px;
    }
    
    /* Logo */
    .logo {
        display: flex;
        align-items: center;
        gap: 8px;
        text-decoration: none;
        flex-shrink: 0;
    }
    .logo-icon { font-size: 1.8rem; }
    .logo-text {
        font-size: 1.3rem;
        font-weight: 700;
        color: white;
    }
    .logo-accent { color: #f97316; }
    
    /* Nav Menu */
    .nav-menu {
        display: flex;
        align-items: center;
        gap: 8px;
        list-style: none;
        margin: 0;
        padding: 0;
    }
    .nav-link {
        color: rgba(255,255,255,0.85);
        text-decoration: none;
        padding: 10px 16px;
        border-radius: 8px;
        font-weight: 500;
        font-size: 0.95rem;
        transition: all 0.2s;
    }
    .nav-link:hover {
        color: white;
        background: rgba(249, 115, 22, 0.2);
    }
    
    /* Dropdown Menu */
    .nav-dropdown { position: relative; }
    .dropdown-toggle i { font-size: 0.7rem; margin-left: 4px; }
    .dropdown-menu {
        position: absolute;
        top: 100%;
        left: 0;
        background: white;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        min-width: 180px;
        padding: 8px 0;
        opacity: 0;
        visibility: hidden;
        transform: translateY(10px);
        transition: all 0.2s;
    }
    .nav-dropdown:hover .dropdown-menu {
        opacity: 1;
        visibility: visible;
        transform: translateY(0);
    }
    .dropdown-menu a {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 12px 20px;
        color: #374151;
        text-decoration: none;
        transition: all 0.2s;
    }
    .dropdown-menu a:hover {
        background: #fff7ed;
        color: #f97316;
    }
    .dropdown-menu a i { width: 18px; }
    
    /* Nav Actions */
    .nav-actions {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-left: auto;
    }
    .nav-icon-btn {
        width: 40px;
        height: 40px;
        border: none;
        background: rgba(255,255,255,0.1);
        color: white;
        border-radius: 10px;
        cursor: pointer;
        transition: all 0.2s;
    }
    .nav-icon-btn:hover {
        background: rgba(249, 115, 22, 0.3);
        color: #f97316;
    }
    
    /* Login Link - Nhỏ gọn */
    .nav-login-link {
        display: flex;
        align-items: center;
        gap: 6px;
        color: rgba(255,255,255,0.7);
        text-decoration: none;
        font-size: 0.85rem;
        padding: 8px 12px;
        border-radius: 8px;
        transition: all 0.2s;
    }
    .nav-login-link:hover {
        color: white;
        background: rgba(255,255,255,0.1);
    }
    .nav-login-link i { font-size: 0.9rem; }
    
    /* CTA Button - Nổi bật */
    .btn-order-cta {
        display: flex;
        align-items: center;
        gap: 8px;
        background: linear-gradient(135deg, #FF6B35, #E55A2B);
        color: white;
        text-decoration: none;
        padding: 12px 20px;
        border-radius: 25px;
        font-weight: 600;
        font-size: 0.95rem;
        box-shadow: 0 4px 15px rgba(249, 115, 22, 0.4);
        transition: all 0.3s;
        position: relative;
        animation: pulse-glow 2s infinite;
    }
    .btn-order-cta:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 25px rgba(249, 115, 22, 0.5);
    }
    .btn-order-badge {
        background: #fef3c7;
        color: #d97706;
        font-size: 0.7rem;
        font-weight: 700;
        padding: 2px 6px;
        border-radius: 10px;
    }
    /* Icon Giỏ hàng riêng */
    .nav-cart-btn {
        position: relative !important;
        display: flex !important;
        align-items: center !important;
        justify-content: center !important;
        width: 42px !important;
        height: 42px !important;
        background: rgba(255,255,255,0.15) !important;
        border-radius: 50% !important;
        color: white !important;
        font-size: 1.1rem !important;
        transition: all 0.3s !important;
        text-decoration: none !important;
    }
    .nav-cart-btn:hover {
        background: #f97316 !important;
        transform: scale(1.05) !important;
    }
    .cart-count {
        position: absolute;
        top: -2px;
        right: -2px;
        background: #ef4444;
        color: white;
        font-size: 0.65rem;
        min-width: 18px;
        height: 18px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
    }
    @@keyframes pulse-glow {
        0%, 100% { box-shadow: 0 4px 15px rgba(249, 115, 22, 0.4); }
        50% { box-shadow: 0 4px 25px rgba(249, 115, 22, 0.6); }
    }
    
    /* Promo Bar */
    .header-promo-bar {
        background: linear-gradient(90deg, #f97316, #ea580c);
        color: white;
        text-align: center;
        padding: 6px 20px;
        font-size: 0.8rem;
        font-weight: 500;
    }
    .promo-divider {
        margin: 0 15px;
        opacity: 0.5;
    }
    
    /* Search Dropdown */
    .search-dropdown {
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: white;
        padding: 20px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        display: none;
    }
    .search-dropdown.active { display: block; }
    .search-input-wrapper {
        max-width: 600px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        gap: 12px;
        background: #f3f4f6;
        padding: 12px 20px;
        border-radius: 30px;
    }
    .search-input-wrapper input {
        flex: 1;
        border: none;
        background: none;
        font-size: 1rem;
        outline: none;
    }
    .search-close {
        background: none;
        border: none;
        cursor: pointer;
        color: #6b7280;
    }
    
    /* Mobile Menu */
    .mobile-menu-btn {
        display: none;
        width: 44px;
        height: 44px;
        border: none;
        background: rgba(255,255,255,0.1);
        color: white;
        border-radius: 10px;
        cursor: pointer;
        font-size: 1.2rem;
    }
    .mobile-menu-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0,0,0,0.5);
        z-index: 1001;
    }
    .mobile-menu-overlay.active { display: block; }
    .mobile-menu {
        position: fixed;
        top: 0;
        right: -300px;
        width: 300px;
        height: 100vh;
        background: white;
        z-index: 1002;
        transition: right 0.3s ease;
        display: flex;
        flex-direction: column;
    }
    .mobile-menu.active { right: 0; }
    .mobile-menu-header {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 20px;
        border-bottom: 1px solid #e5e7eb;
        font-weight: 600;
    }
    .mobile-menu-header button {
        margin-left: auto;
        background: none;
        border: none;
        font-size: 1.2rem;
        cursor: pointer;
    }
    .mobile-nav {
        flex: 1;
        padding: 20px;
    }
    .mobile-nav a {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 15px 0;
        color: #374151;
        text-decoration: none;
        border-bottom: 1px solid #f3f4f6;
    }
    .mobile-nav a i { width: 20px; color: #f97316; }
    .mobile-cart-badge {
        background: #ef4444;
        color: white;
        font-size: 0.7rem;
        padding: 2px 8px;
        border-radius: 10px;
        margin-left: auto;
    }
    .mobile-order-btn {
        margin: 20px;
        padding: 15px;
        background: linear-gradient(135deg, #FF6B35, #E55A2B);
        color: white;
        text-align: center;
        text-decoration: none;
        border-radius: 12px;
        font-weight: 600;
    }
    .mobile-login {
        padding: 0 20px 20px;
    }
    .mobile-login-btn {
        display: block;
        text-align: center;
        padding: 12px;
        border: 2px solid #e5e7eb;
        border-radius: 10px;
        color: #374151;
        text-decoration: none;
    }
    
    /* User Menu Styles */
    .user-menu-item { position: relative; }
    .user-dropdown-nav { position: relative; }
    .user-nav-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        background: none;
        border: none;
        cursor: pointer;
        padding: 8px;
        border-radius: 50%;
        transition: all 0.2s;
        color: white;
    }
    .user-nav-btn:hover { 
        background: rgba(249, 115, 22, 0.2);
    }
    .user-icon {
        font-size: 1.5rem;
        color: #f97316;
    }
    .user-dropdown-content {
        position: absolute;
        top: 100%;
        right: 0;
        background: white;
        min-width: 220px;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        padding: 0;
        display: none;
        z-index: 1000;
        overflow: hidden;
    }
    .user-dropdown-nav:hover .user-dropdown-content,
    .user-dropdown-content:hover { display: block; }
    .user-dropdown-header {
        background: linear-gradient(135deg, #fff7ed, #ffedd5);
        padding: 15px 20px;
        display: flex;
        flex-direction: column;
    }
    .user-greeting {
        font-size: 0.8rem;
        color: #9ca3af;
    }
    .user-name {
        font-size: 1rem;
        font-weight: 600;
        color: #f97316;
    }
    .user-dropdown-content a {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 12px 20px;
        color: #374151;
        text-decoration: none;
        transition: all 0.2s;
    }
    .user-dropdown-content a:hover {
        background: #fff7ed;
        color: #f97316;
    }
    .user-dropdown-content a i {
        width: 18px;
        text-align: center;
    }
    .logout-link:hover {
        background: #fef2f2 !important;
        color: #ef4444 !important;
    }
    .user-dropdown-content hr {
        margin: 0;
        border: none;
        border-top: 1px solid #e5e7eb;
    }
    
    /* Responsive */
    @@media (max-width: 1024px) {
        .nav-menu { display: none; }
        .nav-login-link span { display: none; }
        .btn-order-text { display: none; }
        .btn-order-cta { padding: 12px 16px; }
        .mobile-menu-btn { display: flex; align-items: center; justify-content: center; }
    }
    @@media (max-width: 640px) {
        .nav-actions { gap: 8px; }
        .nav-login-link { display: none; }
        .header-promo-bar { font-size: 0.7rem; }
        .promo-divider { margin: 0 8px; }
    }
    </style>

    <!-- Scroll to Top Button -->
    <button class="scroll-top" id="scrollTopBtn" onclick="scrollToTop()">
        <i class="fas fa-arrow-up"></i>
    </button>

    <script>
        // Header scroll effect
        let lastScroll = 0;
        window.addEventListener('scroll', () => {
            const header = document.querySelector('.header');
            const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
            
            if (scrollTop > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
            
            // Show/hide scroll to top button
            const scrollBtn = document.getElementById('scrollTopBtn');
            if (scrollTop > 500) {
                scrollBtn.classList.add('visible');
            } else {
                scrollBtn.classList.remove('visible');
            }
            
            lastScroll = scrollTop;
        });
        
        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // Check login status and update UI
        function updateLoginUI() {
            const user = JSON.parse(localStorage.getItem('user') || '{}');
            const loginLink = document.getElementById('loginLink');
            const userMenu = document.getElementById('userMenu');
            const adminLink = document.getElementById('adminLink');
            const navUserName = document.getElementById('navUserName');
            
            if (user && user.email) {
                // Đã đăng nhập - ẩn nút đăng nhập, hiện menu user
                if (loginLink) loginLink.style.display = 'none';
                if (userMenu) userMenu.style.display = 'block';
                if (navUserName) navUserName.textContent = user.name || user.email;
                
                // Hiện link Admin nếu là admin/quanly/nhanvien
                const role = (user.role || '').toLowerCase();
                if (adminLink && ['admin', 'quanly', 'nhanvien', 'manager', 'staff'].includes(role)) {
                    adminLink.style.display = 'flex';
                }
            } else {
                // Chưa đăng nhập
                if (loginLink) loginLink.style.display = 'block';
                if (userMenu) userMenu.style.display = 'none';
            }
        }
        
        function logoutUser() {
            localStorage.removeItem('user');
            localStorage.removeItem('cart');
            window.location.href = '/';
        }
        
        // Run on page load
        updateLoginUI();

        // Mobile Menu Toggle
        function toggleMobileMenu() {
            const menu = document.getElementById('mobileMenu');
            const overlay = document.getElementById('mobileMenuOverlay');
            menu.classList.toggle('active');
            overlay.classList.toggle('active');
            document.body.style.overflow = menu.classList.contains('active') ? 'hidden' : '';
        }

        // Update cart count on page load
        function updateCartBadge() {
            const cart = JSON.parse(localStorage.getItem('cart') || '[]');
            const count = cart.reduce((sum, item) => sum + item.quantity, 0);
            const badges = document.querySelectorAll('#cartCount, #cartCountMobile');
            badges.forEach(badge => {
                if (badge) badge.textContent = count;
            });
        }
        updateCartBadge();

        // Global Search
        let searchProducts = [];
        let searchTimeout;

        function toggleSearch() {
            const dropdown = document.getElementById('searchDropdown');
            const toggle = document.getElementById('searchToggle');
            const input = document.getElementById('globalSearch');
            
            dropdown.classList.toggle('active');
            toggle.classList.toggle('active');
            
            if (dropdown.classList.contains('active')) {
                input.focus();
                loadSearchProducts();
            } else {
                input.value = '';
                document.getElementById('searchResults').innerHTML = '';
            }
        }

        // Close search when clicking outside
        document.addEventListener('click', function(e) {
            const searchArea = document.querySelector('.nav-search');
            const searchDropdown = document.getElementById('searchDropdown');
            if (searchArea && !searchArea.contains(e.target)) {
                if (searchDropdown) searchDropdown.classList.remove('active');
                const searchToggle = document.getElementById('searchToggle');
                if (searchToggle) searchToggle.classList.remove('active');
            }
        });

        // Load products for search
        async function loadSearchProducts() {
            if (searchProducts.length > 0) return;
            try {
                const response = await fetch('/data/products.json');
                searchProducts = await response.json();
            } catch (e) {
                console.error('Error loading products:', e);
            }
        }

        // Search input handler
        document.getElementById('globalSearch')?.addEventListener('input', function() {
            clearTimeout(searchTimeout);
            searchTimeout = setTimeout(() => performSearch(this.value), 300);
        });

        function performSearch(query) {
            const resultsContainer = document.getElementById('searchResults');
            
            if (!query || query.length < 2) {
                resultsContainer.innerHTML = '<div class="search-hint">Nhập ít nhất 2 ký tự để tìm kiếm...</div>';
                return;
            }

            const q = query.toLowerCase();
            const results = searchProducts.filter(p => 
                p.name.toLowerCase().includes(q) || 
                (p.description && p.description.toLowerCase().includes(q))
            ).slice(0, 6);

            if (results.length === 0) {
                resultsContainer.innerHTML = '<div class="search-no-result">Không tìm thấy món ăn phù hợp</div>';
                return;
            }

            resultsContainer.innerHTML = results.map(p => `
                <a href="/ThucDon" class="search-result-item" onclick="goToProduct('${p.code}')">
                    <img src="/images/products/${p.image}" alt="${p.name}" onerror="this.src='/images/products/MenuItem_M00012.webp'" />
                    <div class="search-result-info">
                        <span class="search-result-name">${highlightText(p.name, query)}</span>
                        <span class="search-result-price">${p.price}</span>
                    </div>
                </a>
            `).join('') + `
                <a href="/ThucDon?search=${encodeURIComponent(query)}" class="search-view-all">
                    Xem tất cả kết quả <i class="fas fa-arrow-right"></i>
                </a>
            `;
        }

        function highlightText(text, query) {
            const regex = new RegExp(`(${query})`, 'gi');
            return text.replace(regex, '<mark>$1</mark>');
        }

        function goToProduct(code) {
            toggleSearch();
            window.location.href = '/ThucDon?product=' + code;
        }

        // Keyboard shortcut: Ctrl+K to open search
        document.addEventListener('keydown', function(e) {
            if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
                e.preventDefault();
                toggleSearch();
            }
            if (e.key === 'Escape') {
                document.getElementById('searchDropdown').classList.remove('active');
                document.getElementById('searchToggle').classList.remove('active');
            }
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)

    <!-- AI Chatbot Widget -->
    <div class="chatbot-widget" id="chatbotWidget">
        <button class="chatbot-toggle" id="chatbotToggle" onclick="toggleChatbot()">
            <span class="chatbot-icon">🤖</span>
            <span class="chatbot-close">✕</span>
        </button>
        
        <div class="chatbot-container" id="chatbotContainer">
            <div class="chatbot-header">
                <div class="chatbot-avatar">🌶️</div>
                <div class="chatbot-info">
                    <h4>Trợ lý Sasin</h4>
                    <span class="chatbot-status">● Đang hoạt động</span>
                </div>
                <button class="chatbot-minimize" onclick="toggleChatbot()">−</button>
            </div>
            
            <div class="chatbot-messages" id="chatbotMessages">
                <div class="chat-message bot">
                    <div class="message-avatar">🌶️</div>
                    <div class="message-content">
                        <p>Xin chào! 👋 Tôi là trợ lý ảo của Mỳ Cay Sasin. Tôi có thể giúp bạn:</p>
                        <ul>
                            <li>🍜 Gợi ý món ăn phù hợp</li>
                            <li>🌶️ Tư vấn cấp độ cay</li>
                            <li>🎁 Giới thiệu combo tiết kiệm</li>
                        </ul>
                        <p>Bạn muốn tôi giúp gì nào?</p>
                    </div>
                </div>
            </div>
            
            <div class="chatbot-suggestions" id="chatbotSuggestions">
                <button onclick="sendSuggestion('Gợi ý món best seller')">🔥 Best seller</button>
                <button onclick="sendSuggestion('Tôi muốn ăn cay vừa')">🌶️ Cay vừa</button>
                <button onclick="sendSuggestion('Combo cho 2 người')">👫 Combo 2 người</button>
                <button onclick="sendSuggestion('Món nào không cay?')">😊 Không cay</button>
            </div>
            
            <div class="chatbot-input">
                <input type="text" id="chatbotInput" placeholder="Nhập tin nhắn..." onkeypress="handleChatKeypress(event)" />
                <button onclick="sendChatMessage()" id="chatSendBtn">
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
        </div>
    </div>

    <style>
    /* Chatbot Widget Styles */
    .chatbot-widget {
        position: fixed;
        bottom: 20px;
        right: 20px;
        z-index: 9999;
        font-family: 'Be Vietnam Pro', sans-serif;
    }
    .chatbot-toggle {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: linear-gradient(135deg, #f97316, #ea580c);
        border: none;
        cursor: pointer;
        box-shadow: 0 4px 20px rgba(249, 115, 22, 0.4);
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        position: relative;
    }
    .chatbot-toggle:hover {
        transform: scale(1.1);
        box-shadow: 0 6px 25px rgba(249, 115, 22, 0.5);
    }
    .chatbot-icon {
        font-size: 1.8rem;
        transition: all 0.3s ease;
    }
    .chatbot-close {
        font-size: 1.5rem;
        color: white;
        position: absolute;
        opacity: 0;
        transform: rotate(-90deg);
        transition: all 0.3s ease;
    }
    .chatbot-widget.active .chatbot-icon {
        opacity: 0;
        transform: rotate(90deg);
    }
    .chatbot-widget.active .chatbot-close {
        opacity: 1;
        transform: rotate(0);
    }
    .chatbot-container {
        position: absolute;
        bottom: 75px;
        right: 0;
        width: 380px;
        max-height: 550px;
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        display: none;
        flex-direction: column;
        overflow: hidden;
        animation: chatbotSlideIn 0.3s ease;
    }
    .chatbot-widget.active .chatbot-container {
        display: flex;
    }
    @@keyframes chatbotSlideIn {
        from { opacity: 0; transform: translateY(20px) scale(0.95); }
        to { opacity: 1; transform: translateY(0) scale(1); }
    }
    .chatbot-header {
        background: linear-gradient(135deg, #f97316, #ea580c);
        color: white;
        padding: 15px 20px;
        display: flex;
        align-items: center;
        gap: 12px;
    }
    .chatbot-avatar {
        width: 45px;
        height: 45px;
        background: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    .chatbot-info h4 {
        margin: 0;
        font-size: 1rem;
        font-weight: 700;
    }
    .chatbot-status {
        font-size: 0.75rem;
        opacity: 0.9;
    }
    .chatbot-minimize {
        margin-left: auto;
        background: rgba(255,255,255,0.2);
        border: none;
        color: white;
        width: 30px;
        height: 30px;
        border-radius: 50%;
        cursor: pointer;
        font-size: 1.2rem;
    }
    .chatbot-messages {
        flex: 1;
        overflow-y: auto;
        padding: 15px;
        max-height: 320px;
        background: #f9fafb;
    }
    .chat-message {
        display: flex;
        gap: 10px;
        margin-bottom: 15px;
        animation: messageSlide 0.3s ease;
    }
    @@keyframes messageSlide {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .chat-message.user {
        flex-direction: row-reverse;
    }
    .message-avatar {
        width: 35px;
        height: 35px;
        background: #fff;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.1rem;
        flex-shrink: 0;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .chat-message.user .message-avatar {
        background: #f97316;
        color: white;
    }
    .message-content {
        background: white;
        padding: 12px 16px;
        border-radius: 18px;
        max-width: 75%;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .chat-message.user .message-content {
        background: linear-gradient(135deg, #f97316, #ea580c);
        color: white;
    }
    .message-content p {
        margin: 0 0 8px 0;
        line-height: 1.5;
        font-size: 0.9rem;
    }
    .message-content p:last-child {
        margin-bottom: 0;
    }
    .message-content ul {
        margin: 8px 0;
        padding-left: 20px;
    }
    .message-content li {
        font-size: 0.85rem;
        margin-bottom: 4px;
    }
    .chatbot-suggestions {
        padding: 10px 15px;
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
        background: white;
        border-top: 1px solid #e5e7eb;
    }
    .chatbot-suggestions button {
        padding: 8px 14px;
        background: #fff7ed;
        border: 1px solid #fed7aa;
        border-radius: 20px;
        font-size: 0.8rem;
        cursor: pointer;
        transition: all 0.2s;
        white-space: nowrap;
    }
    .chatbot-suggestions button:hover {
        background: #f97316;
        color: white;
        border-color: #f97316;
    }
    .chatbot-input {
        padding: 15px;
        display: flex;
        gap: 10px;
        background: white;
        border-top: 1px solid #e5e7eb;
    }
    .chatbot-input input {
        flex: 1;
        padding: 12px 16px;
        border: 2px solid #e5e7eb;
        border-radius: 25px;
        font-size: 0.9rem;
        outline: none;
        transition: border-color 0.2s;
    }
    .chatbot-input input:focus {
        border-color: #f97316;
    }
    .chatbot-input button {
        width: 45px;
        height: 45px;
        background: linear-gradient(135deg, #f97316, #ea580c);
        border: none;
        border-radius: 50%;
        color: white;
        cursor: pointer;
        transition: all 0.2s;
    }
    .chatbot-input button:hover {
        transform: scale(1.05);
    }
    .chatbot-input button:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }
    .typing-indicator {
        display: flex;
        gap: 4px;
        padding: 8px 0;
    }
    .typing-indicator span {
        width: 8px;
        height: 8px;
        background: #9ca3af;
        border-radius: 50%;
        animation: typing 1.4s infinite;
    }
    .typing-indicator span:nth-child(2) { animation-delay: 0.2s; }
    .typing-indicator span:nth-child(3) { animation-delay: 0.4s; }
    @@keyframes typing {
        0%, 60%, 100% { transform: translateY(0); }
        30% { transform: translateY(-8px); }
    }
    
    /* Mobile responsive */
    @@media (max-width: 480px) {
        .chatbot-container {
            width: calc(100vw - 40px);
            right: -10px;
            max-height: 70vh;
        }
    }
    </style>

    <script>
    // Chatbot Functions
    function toggleChatbot() {
        const widget = document.getElementById('chatbotWidget');
        widget.classList.toggle('active');
        
        if (widget.classList.contains('active')) {
            document.getElementById('chatbotInput').focus();
        }
    }

    function handleChatKeypress(e) {
        if (e.key === 'Enter') {
            sendChatMessage();
        }
    }

    function sendSuggestion(text) {
        document.getElementById('chatbotInput').value = text;
        sendChatMessage();
    }

    async function sendChatMessage() {
        const input = document.getElementById('chatbotInput');
        const message = input.value.trim();
        
        if (!message) return;
        
        // Add user message
        addMessage(message, 'user');
        input.value = '';
        
        // Hide suggestions after first message
        document.getElementById('chatbotSuggestions').style.display = 'none';
        
        // Show typing indicator
        const typingId = showTyping();
        
        // Disable send button
        const sendBtn = document.getElementById('chatSendBtn');
        sendBtn.disabled = true;
        
        try {
            const response = await fetch('/api/chatbot/chat', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ message: message })
            });
            
            const data = await response.json();
            
            // Remove typing indicator
            removeTyping(typingId);
            
            // Add bot response
            addMessage(data.reply, 'bot');
        } catch (error) {
            removeTyping(typingId);
            addMessage('Xin lỗi, có lỗi xảy ra. Vui lòng thử lại! 🙏', 'bot');
        }
        
        sendBtn.disabled = false;
    }

    function addMessage(text, sender) {
        const container = document.getElementById('chatbotMessages');
        const avatar = sender === 'bot' ? '🌶️' : '👤';
        
        // Convert markdown-like formatting
        let formattedText = text
            .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
            .replace(/\n/g, '<br>')
            .replace(/• /g, '• ');
        
        const messageHtml = `
            <div class="chat-message ${sender}">
                <div class="message-avatar">${avatar}</div>
                <div class="message-content">
                    <p>${formattedText}</p>
                </div>
            </div>
        `;
        
        container.insertAdjacentHTML('beforeend', messageHtml);
        container.scrollTop = container.scrollHeight;
    }

    function showTyping() {
        const container = document.getElementById('chatbotMessages');
        const id = 'typing-' + Date.now();
        
        const typingHtml = `
            <div class="chat-message bot" id="${id}">
                <div class="message-avatar">🌶️</div>
                <div class="message-content">
                    <div class="typing-indicator">
                        <span></span><span></span><span></span>
                    </div>
                </div>
            </div>
        `;
        
        container.insertAdjacentHTML('beforeend', typingHtml);
        container.scrollTop = container.scrollHeight;
        
        return id;
    }

    function removeTyping(id) {
        const typing = document.getElementById(id);
        if (typing) typing.remove();
    }
    </script>
</body>
</html>
